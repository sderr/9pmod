To build the modules

export KERNEL_HEADERS=/path/to/kernel_tree_or_headers
make

Then:

make install will install the modules in /lib

Or:

make localinstall will install the modules in 'modules'. Plenty of warnings, that is normal.

